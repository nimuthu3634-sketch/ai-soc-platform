# Runtime Branding Assets

Place the primary Aegis Core logo here for the frontend shell:

- `public/branding/aegiscore-logo.png`

The React application already references `/branding/aegiscore-logo.png` and falls back to a text-based brand mark if the logo file is not available yet.
