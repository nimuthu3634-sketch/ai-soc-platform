# Branding Assets

Place the proposal cover image for the academic documentation here:

- `docs/branding/proposal-cover.png`

The application scaffold references this path in documentation and settings metadata, but it does not require the file to exist during initial setup.
